.vscode/**
.vscode-test/**
intellij-plugin/**
src/**
out/cli/tests/**
.gitignore
.yarnrc
vsc-extension-quickstart.md
**/tsconfig.json
**/eslint.config.mjs
**/*.map
**/*.ts
**/.vscode-test.*
# Local workspace state — the code index carries file paths and code previews
# from whatever machine packaged the VSIX, so it must never ship.
.olliberty/**
.ollama/**
# Build and release plumbing the extension does not need at runtime.
.github/**
.npmignore
Makefile
setup.sh
scripts/**
tsconfig.cli.json
dist/**
*.vsix
